# -*- coding: utf-8 -*-
# from django.conf.urls import patterns, url, include
# from connect.api import userinfo, token, reg, pubkey

# urlpatterns = patterns(
#     '',
#     url(r'', include(pubkey.AuthorityKeyResource().urls)),
#     url(r'', include(reg.RegResource().urls)),
#     url(r'', include(token.TokenResource().urls)),
#     url(r'', include(userinfo.UserInfoResource().urls)),
# )
urlpatterns = [
]
